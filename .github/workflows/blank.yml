name: CI
on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]


  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Тест на скрипт Filesdeleter.sh
        run: |
          chmod +x TestFilesdeleter.sh
          ./TestFilesdeleter.sh
